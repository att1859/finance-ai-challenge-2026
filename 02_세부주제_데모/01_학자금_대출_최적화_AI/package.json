{
  "name": "student-consumption-smoothing-ai",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "test": "node --test tests/*.test.js",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1"
  },
  "devDependencies": {
    "vite": "^7.1.3"
  },
  "dependencies": {
    "@seed-design/css": "^2.5.0",
    "@seed-design/vite-plugin": "^2.1.0"
  }
}
