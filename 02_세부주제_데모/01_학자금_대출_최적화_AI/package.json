{
  "name": "student-consumption-smoothing-ai",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "node --env-file-if-exists=.env.local scripts/dev.js",
    "dev:ui": "vite --host 127.0.0.1 --configLoader native",
    "dev:api": "node --env-file-if-exists=.env.local server/index.js",
    "test": "node --test",
    "test:ui": "playwright test",
    "build": "vite build --configLoader native",
    "preview": "vite preview --host 127.0.0.1 --configLoader native"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "vite": "^7.1.3"
  }
}
